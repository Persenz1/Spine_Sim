# M3 持续总预载动力学未闭合问题

1. 仓库仍缺少执行文档引用的
   `04_爪刺仿真器_失败问题总结与重启约束.md`。实现遵守现有项目执行版和
   M2 交接中的同类禁令，但无法核对缺失文件的额外条款。
2. M1/M2 尚无项目随机地形的二维杆体/锥段只读净空接口。动态 M3 解析验收因而
   保留 `model_state=parameter_unclosed`，不能用于正式排名或 M4 能力下包络。
3. M2 正式第一轮未运行，`M2_selected_parameter_packs_proposal.yaml` 为空且待批准。
   M3 不能判断安装角、梯度角、阵列方向或柔顺的正式优劣。
4. `full_chain_frozen_manifest.json` 不存在，M4 尚未完成；正式 M3 第一轮门禁未打开。
5. `m3.1.0` 首版只开放共同背板 Z 动力学；水平 x 为规定运动，俯仰/横滚显式
   锁定，故不含背板转动、翘曲、安装误差、局部 y 摩擦或损伤。开放转动前必须
   冻结相应惯量和阻尼并升级模型版本。
6. 背板质量/阻尼、针模态质量/阻尼、冲量接触参数及内部时间步目前只有解析夹具
   值，没有项目标定来源。它们必须写入 `unclosed_parameter_names`，因此动态
   验收即使通过也保持 `formal_ranking_eligible=false`。
7. 10 μm 离散支撑切换会影响冲击事件时刻和峰值。正式最终候选必须做内部时间步
   减半、接触参数敏感性及 5 μm 地形离散收敛复核；冲击峰不得并入稳态承载统计。
8. 载荷共享指标是工程降阶量：normal、\(|F_x|\) 和合力分别加权且所有零载荷名义针
   进入分母。它们不能替代逐针路径、事件和原始 wrench。
