# M2 未闭合问题

1. 仓库缺少 M2 执行文档引用的
   `04_爪刺仿真器_失败问题总结与重启约束.md`。当前实现已遵守现有执行版中列出的
   同类冻结禁令，但无法核对缺失文档里的额外条款。
2. 针体材料牌号和标定参数未冻结。默认使用显式标记的
   `E=200 GPa, ν=0.29` 高碳钢代理；正式比较必须保存该假设并做敏感性。
3. 项目随机地形的二维杆体/锥段净空接口未闭合。解析夹具显式关闭该检查；
   十地形 smoke 保留 `parameter_unclosed`，不能进入正式参数排名。
4. 接触模型是无损伤、准静态、主平面降阶模型，不包含压痕、剥落、磨损、碎屑、
   针尖转动自由度或真实材料标定。
5. Coulomb 分支在有限载荷安全界内若没有可接纳的准静态根，返回
   `no_admissible_contact_equilibrium/model_unclosed`，不会用罚刚度伪造解。
   正式 campaign 前应通过少量项目地形复核该事件的频率和位置。
6. 正式 M2 第一轮尚未获准启动；不存在参数排名、半径缩减、有效刚度区间或
   M3 入选包结论。
