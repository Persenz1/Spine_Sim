# 下一窗口提示词：直接完成单刺与阵列统一实现

请在 `D:\Code\Spine_Sim` 的当前工作树直接完成新版机理下 canonical 单刺与阵列迁移。
使用 `gpt-5.6-sol`、推理强度 `ultra`，持续工作到实现、测试、旧路径删除和文档闭环全部完成，
不要停在设计、接口或脚手架。

先完整阅读：

1. `docs/engineering/单刺与阵列统一仿真实施指导.md`；
2. `docs/engineering/工程仿真参数继承表.md`；
3. `docs/theory/2026-08-29_主稿修订审查.md`；
4. `docs/theory/README.md` 指向的当前主稿与验算稿，重点 geometry、第2–6章单刺、第8–9章
   阵列、第13章及附录C/D/F；只有公式争议才查逐篇证据卡。

这只是阅读顺序；物理/公式冲突严格按最新主稿 > 验算稿 > 证据卡裁决。修订审查和参数表只做
软件影响摘要与 legacy 输入映射，不能覆盖主稿。

明确忽略《钩爪抓附机理逐章讲义_大学水平离线版》：它未归档、不是规范源，不得查找、搬入、
引用或用于实现裁决。旧网页端 Pro handoff 已废止，不得从 Git 历史恢复。此前中断任务没有代码
或提交，不需要寻找或合并其工作树。

新版机理负责物理语义；原工程的坐标/边界语义、候选设计轴和已运行 preset 输入必须无损迁移，
但不得升格为唯一硬件定型。先建立 machine-readable canonical parameter registry 和逐字段 SI
迁移测试，再替换旧路径。不得静默改变坐标、针尖半径、针径、固定角 `Le=4 mm`、梯度角
`L_j=4 sin80°/sinα_j mm`、安装角/角度列、阵列形状与4/5/6 mm 间距、单边
弹簧与4 mm 行程、持续预载、拖拽协议、材料/摩擦回归值、既有 protocol 或12个终筛 legacy
design ID。原 ID 只作 provenance，新版 case/result identity 必须另行版本化。
旧资料的角度以 degree 展示，canonical schema/result 必须用 rad：主稿 β/ψ 是 pitch/yaw，字段
映射为 `pitch_rad=-deg2rad(alpha_old_deg)`、`yaw_rad=deg2rad(beta_yaw_old_deg)=0`；旧60→80°
保持列顺序并迁成 pitch -60→-80°，
不能翻转列或把旧 β 当主稿俯仰。0.8 mm 是针杆直径，不是初始间隙。按参数表区分
`frozen_design_semantics`、`candidate_design_value`、`legacy_regression`、`numerical_protocol`、
`legacy_analysis` 和 `unclosed`；旧 E/ν/摩擦
值只进入命名的 legacy regression 配置，不能升格为无来源的全局默认。缺少标定不能靠改默认值
补齐。旧结果的物理输入是回归证据，旧物理
响应、优选角色和排名不是新版真值。
机器 final 的 C6/A3/A4/C5/C2/C7 与人工报告的 A1/A2/A3/A5（第五才A4）选择并不一致，正好
证明没有定型样机；两者都只能作为命名 legacy preset/provenance。
旧 M3-A/M3-B 只有源码 generator 和数量测试、没有归档运行 manifest，登记为 source-defined
candidate preset，不得声称已运行；coarse 与 fine 也必须保持两个不同 campaign identity。
历史十字对爪的40 mm 半跨和50 mm 外载点只迁入 registry 供未来重算，本轮不实现整爪，也不
把通用阵列接口绑定到十字形。

先跑现有小测试并审计 terrain→geometry，随后不停顿地继续实现。保留高度场、材料随机场、
`TerrainLibrary`、有限球尖包络和现有可选 CPU/CUDA terrain 后端；不下载砂纸扫描、不重写材料
算法。保留 P40/P100/P240 单样本 `partially_validated`、其他砂纸及砖/混凝土 `provisional` 的
status/provenance，不得冒充总体标定。修复完整针尖 footprint 的 valid 传播、实测上下界/
不确定性、缓存版本与 identity，以及
旧消费端丢失 support、near-tie、y 向几何和门控的问题。建立统一 `ContactCandidate`、
`query_next_candidate(...)` 和路径延拓驱动；区分 surface/envelope/contact normal，support 切换
不得线性插值，near-tie 不得强压成唯一法向，rod-clearance 必须使用完整针姿态。

在 `src/spine_sim` 内完成唯一 `geometry → single_spine → array` 生产链：

- 统一八态 `SEARCH/CONTACT/STICK/SLIP/DETACH/REBOUND/HARDSTOP/FAILED` 和指导中冻结的
  `EventType`/from-to；必须覆盖 `CONTACT_REJECT`、`RESTICK` 与 `HARDSTOP_RELEASE`。非法候选
  `CONTACT→SEARCH` 不经过 detach/rebound；`REENGAGE` 只表示完成既往 detach/rebound/search
  后的重新接触。物理、数值、模型、运行状态分离；逐刺保存历史。
- `single_spine_quasistatic` 实现三维点接触摩擦锥、二维解析特例、低阶梁/悬架、单边弹簧互补、
  硬限位、trial/accepted、最早事件、wrench/一致切线和条件式容量。缺参精确返回
  `PARAMETER_UNCLOSED`，拓扑不存在返回 `OUT_OF_SCOPE`；不得沿用无来源 `C_i,c_i` 或额外角度
  门槛。`MATERIAL_FAILURE` 按对象/mode 携带 action：只有闭合的灾难性整刺/根部断开可
  `PERMANENT_REMOVE` 后继续重平衡；需要塑性、钝化、剥落或损伤演化的事件在首界
  `STOP_MODEL_LIMIT/OUT_OF_SCOPE`，不得猜测后续。固化工程指导中的 `u=0.702067 mm`
  轴向弹簧互补子夹具；这里只闭合该子问题，不得把整个单刺 case 标成 `CLOSED`。
- `array_rigid_backplate_event` 只能调用 canonical 单刺。实现逐刺 `d_i^0`、任意 pose、共同刚性
  背板6D `q_C`、互斥的 pose/force 混合控制、loader stiffness、尺度化残差/秩判断、活动集、最早
  事件、式(9-29)力矩感知重平衡、级联和逐刺再挂接。固定墙面时实际背板小位姿为 `-q_C`；
  伪逆不得冒充稳定解；秩可解与稳定性分开。保守分支按指导中的尺度化约束自由子空间
  `lambda_min` 判据接受，SLIP/非对称切线只能标 directional quasistatic 且 dynamic stability
  `OUT_OF_SCOPE`，不得输出 stable=true。装配 O(N)，不得构造 N×N 稠密耦合矩阵。
- 集中定义 `n_nominal/n_geometric/n_contact/n_engaged/n_active`，其中 `n_engaged` 对应理论
  稳定挂接数 `n_e`，`n_active` 对应 `n_eff=|A|`。参数可评估性另用 `n_evaluable` 与未知上下界，
  不得拿它替代 `n_e`。按局部法向反力定义
  `P_sum/P_avg`，零接触时 `P_avg` 未定义。重命名 inverse-Simpson 指标，按历史定义重实现
  `J+/J-/Jnet`；公式和有效路径合同以工程指导为准，missing/invalid 不补零。旧0.25阈值、
  0.50 mm窗口和 duty 只属于 `legacy_analysis`，不进入 solver。不复制全局 `reset/reseat`。

只迁移有价值的小型 run/campaign、paired seeds、manifest/trace Parquet 和渐进采样概念，不整段
照搬旧约2200行流程或旧排名。用临时解析 catalog 完成 CLI、campaign 和 schema smoke，不跑正式
300地形或大 campaign。仿真入口迁到 `spine-sim`，保留独立 `spine-terrain`；提升 project/model/
result schema 与 solver semantics version，把版本和输入哈希纳入 identity，新结果不覆盖旧结果。
等参数、测试和小 campaign 已等价覆盖后，删除 `m3_fast` 物理核、`spine-m3-fast`、全局 reseat、
旧 `Neff` 及只验证旧语义的测试/monitor，不留第二套物理 solver 或双 schema。

CPU 是单刺/阵列权威实现，本轮不新增 CUDA 数值核。不要实现整爪/整机或通用插件体系。真实动态
回弹、损伤演化、背板连续弯曲等按工程指导显式降级；物理速度1 mm/s 仍保留为装置协议元数据，
准静态模型不得声称能给出冲击或惯性结果。

逐项通过工程指导第7节门禁：参数 registry 与12个 design round-trip、footprint-valid 和三类法向、
二维/三维单刺、双向滑移、弹簧互补、固定光滑分支切线、事件单边跳变、单刺退化、两刺闭式、
6D 力矩平衡、秩亏拒绝、逐刺历史、400–3600刺线性装配 smoke、canonical campaign/schema I/O。
先跑最小测试，再跑全部可用 pytest；不能用手工调用替代最终测试。

完成时汇报：保留的工程参数及逐字段差异、terrain 实际修改、canonical 单刺/阵列能力与显式降级、
删除的旧路径、结果 schema、测试通过/跳过/失败数、尚未闭合的真实参数。只要能从当前代码、
参数表和新版机理裁决就直接做，不要再次询问架构。
