# 2026-08-30 单刺与阵列统一实现：收尾交接

## 1. 交接目的

当前窗口已完成 canonical 单刺与阵列迁移的生产实现、旧路径删除、文档更新和完整 CPU 回归。
本文件供下一 Codex 任务做一次独立的提交后复核并处理任何真实发现；不要重新设计或重写已通过
门禁的实现。

工作目录为 `D:\Code\Spine_Sim`，交接分支为 `main`。本文件写完后，上一任务会把当前工作树
提交并推送到 `origin/main`；下一任务应以该已推送提交为起点，并先确认 `HEAD`、upstream 和
工作树状态。

## 2. 必读材料与裁决顺序

先读：

1. 本文件；
2. [`../engineering/2026-08-30_单刺与阵列统一实现报告.md`](../engineering/2026-08-30_单刺与阵列统一实现报告.md)；
3. [`../engineering/单刺与阵列统一仿真实施指导.md`](../engineering/单刺与阵列统一仿真实施指导.md)；
4. [`../engineering/工程仿真参数继承表.md`](../engineering/工程仿真参数继承表.md)；
5. [`../theory/README.md`](../theory/README.md) 指向的当前主稿和验算稿。

若物理或公式冲突，仍按最新主稿 > 验算稿 > 逐篇证据卡裁决。工程指导与参数表负责软件合同和
legacy 输入映射，不覆盖主稿。

旧 `docs/handoff/2026-08-28_网页端Pro工程设计交接.md` 已明确废止并删除，不能从 Git 历史恢复。
《钩爪抓附机理逐章讲义_大学水平离线版》不是规范源，不得查找、搬入或引用。

## 3. 已完成的生产状态

唯一生产链已经是：

```text
terrain / TrackGeometry v2
  → geometry / ContactCandidate
  → single_spine_quasistatic
  → array_rigid_backplate_event
  → canonical-result-2
```

主要完成项：

- 建立 machine-readable canonical 参数 registry，原工程构型、protocol、M3-A 80 个、M3-B 28 个、
  full scan 1344 个输入以及 12 个终筛 ID 均保持顺序和 provenance；角度按明确的 degree→rad 与
  pitch/yaw 符号合同迁移，机器选择和人工选择没有被误合并成定型样机。
- Track 升到 schema 2，纳入完整球 footprint valid、top-2 support、三类法向、x/y 坡度、实测
  上下界、identity/hash 与姿态相关杆体 clearance；v1 cache 明确要求重建。
- 完成统一候选/路径 cursor、八态单刺、二维楔形和三维摩擦、梁/悬架、单边弹簧、硬限位、容量、
  最早事件、trial/commit、wrench 和一致切线。
- 完成任意逐刺 pose/间隙的刚性背板 6D 混合控制、loader stiffness、equality constraints、
  O(N) 装配、rank/range、活动集、全局最早事件、式 (9-29) 重平衡、级联和保守/方向稳定性分类。
- 统一计数、载荷分担和路径积分指标；完成 versioned case/campaign identity、manifest/lineage、
  canonical summary/events/trace 以及带 schema metadata 的无损 Parquet 嵌套列回读。
- 删除 `src/spine_sim/m3_fast/{campaign,model,solver,terrain}.py`、`spine-m3-fast` entrypoint、
  `scripts/monitor_m3_full_scan.py` 和只验证旧语义的 `tests/test_m3_fast.py`。

版本为 `0.3.0`；project/model/result/solver/geometry/registry 版本为 `2`、
`canonical-single-array-1`、`canonical-result-2`、`single-array-event-v1`、
`contact-candidate-2`、`canonical-parameters-1`。

完整细节、逐字段迁移裁决和显式降级见实现报告，不要只凭本交接的摘要重新推断。

## 4. 上一任务的验证证据

Windows 原生 PowerShell、Conda `codex-py312` 环境的完整回归为：

```text
145 passed, 5 skipped, 12 subtests passed
```

没有失败或 xfail。5 个 skip 是 4 个当前没有 CuPy/GPU 的 terrain 检查，以及 1 个本地没有可验证
P100 原始高程文件。CPU 单刺/阵列、geometry、registry、schema、Parquet 和 CLI/campaign 路径均
实际执行。最终提交前还会再次运行完整 pytest、`git diff --check` 和旧生产路径静态搜索。

## 5. 下一任务的限定收尾

下一任务使用 `gpt-5.6-sol`、推理强度 `ultra`，按以下顺序持续完成：

1. 确认交接提交已位于 `main` 且与 `origin/main` 一致，工作树没有遗漏或意外生成物。
2. 独立审阅提交 diff，重点检查参数 registry、terrain→geometry 合同、单刺/阵列状态提交、结果
   schema，以及旧生产路径是否确实只有 provenance/文档提及。
3. 用 `conda run --no-capture-output -n codex-py312 python -m pytest -q -rs` 重跑完整测试；同时运行
   `git diff --check`。如需验证安装元数据，可在现有环境做 editable reinstall 或构建 wheel，但不得
   新建环境、使用 bare Python/pip 或安装 `uv`。
4. 只有发现可复现的真实缺陷才做最小修复，并补精确测试；修复后重跑相关测试和完整 pytest，
   再提交并推送。不要为了“收尾”引入第二套 solver、兼容双 schema、猜测默认参数或正式大 campaign。
5. 若独立复核无发现，直接给出完成报告：提交哈希、验证命令和计数、保留参数、删除路径、显式
   模型边界及未闭合真实输入。不要制造无意义改动。

上一任务在提交中有意保留并纳入了用户此前对 `README`、`docs/theory`、`docs/archive`、主稿审查
及原始资料校验和的修改；不要把它们当成污染回退。也不要恢复已删除的旧 Pro handoff。

## 6. 不能被“补默认值”掩盖的边界

- 真实钢材强度/疲劳、表面弹性/断裂、完整 cap/cone 尺寸、针管净空/摩擦、逐刺装配间隙、三维
  装配误差、悬架耦合/回差、质量和阻尼仍未闭合。
- dynamic stability 固定为 `OUT_OF_SCOPE`；directional status 是当前分支耗散与互补残差的准静态
  单侧代理，不是动态时间积分。
- 整爪/整机、背板连续弯曲、杆体分布接触、塑性/磨损/损伤演化及 CUDA 力学核不在本轮范围。
- 本轮只运行解析小 catalog，没有运行 300 地形或正式大 campaign。

这些是有意、可审计的模型边界，不应通过经验默认值、旧响应复用或扩大完成声明来消除。
