# M0 仓库审查与旧资产处置

审查日期：2026-07-27

## 审查结论

实施前仓库只有现已移入 `docs/archive/legacy_requirements/` 的 M0–M4 提示词和工程背景执行版，没有生产源代码、测试、结果目录、旧 runner、Bundle、Schema 或资源调度代码。因此本次没有复制或继承任何旧实现。

M0 提示词要求的 `04_爪刺仿真器_失败问题总结与重启约束.md` 不在工作树中，Git 当前历史也没有可读取副本。实现采用现有两份权威文档中已明确重述的边界：不把 `preload_infeasible` 用作通用失败，不把拖动脱离翻译成初始预载失败，不把执行异常翻译成物理不可行。

## 处置清单

| 类别 | 结论 |
|---|---|
| 可直接复用的单位、坐标、ID、结果读写、并行代码 | 无；仓库中不存在 |
| 只能逐项参考的事务、Bundle、Schema、调度代码 | 无；仓库中不存在 |
| 旧 `preload_infeasible` 业务语义 | 明确停用；只保留未来 M2 初始预载阶段专用的 `initial_preload_infeasible` 语义边界 |
| 旧生产 runner | 不存在且不兼容；新 runner 只执行独立 case |
| 旧生产结果与 9,600-case 工作区 | 当前仓库未发现；新结果默认写入被 Git 忽略的 `results/`，不会覆盖旧路径 |

## 新代码隔离

- 新包：`src/spine_sim/`
- 测试：`tests/`
- 最小配置：`examples/`
- M0 交付文档：`docs/m0/M0_*.md`

命名空间不包含旧兼容层。未来 M1–M4 应依赖公开 M0 数据结构，而不是反向要求 M0 模拟旧 Bundle 或旧状态事务。
