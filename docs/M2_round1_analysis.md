# M2 fixed-Z 扫描处置记录（已失效）

## 结论

2026-07-28 用户确认预载应为全路径持续恒定外力，针—地形运动必须按动力学处理。
因此旧 fixed-Z 边界下生成的全部 case 及其摩擦、针径、弹簧刚度和角度趋势均不得
用于选型。

## 已产生但作废的数据

- 正式地形：30 个 seed（32001–32030），继续有效；
- 旧第一轮使用：seed 32001–32015；
- 硬件矩阵：60 个单针构型；
- 摩擦档：低/中/高三档；
- case：2700/2700 完成，无执行错误；
- 物理模型：`m2.0.0/project_model_P_main_plane_quasistatic_v1`；
- 失效原因：初始预载后固定安装座 Z，未持续施加外部恒载，也未处理脱离—冲击—
  再接触动力学。

这些数据统一解释为 `legacy_fixed_z_invalid_for_ranking`。它们只可用于文件格式、
吞吐、断点续跑和旧模型诊断。

## 下一次 M2 第一轮的门禁

1. `M2_DYNAMIC_CONSTANT_PRELOAD_SPEC.md` 对应代码完成；
2. 动态解析门禁和时间步/接触参数收敛通过；
3. 安装座质量、阻尼和动态接触参数冻结；
4. 用户审查新版少量地形力—距离、Z—距离和事件图；
5. 重新设计参数矩阵后再开始正式扫描。

在上述门禁前，不产生任何 M2 入选参数包。
