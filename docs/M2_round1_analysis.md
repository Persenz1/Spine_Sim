# M2 第一轮分析

**状态：未运行，等待门禁与用户批准。**

当前只有解析验收和十地形 smoke。它们不能用于主效应、配对 seed 差值、
bootstrap 排名或 Pareto 稳定性分析。

## 停止门

以下前置条件尚未满足：

- M3 未完成；
- M4 未完成；
- `full_chain_frozen_manifest.json` 不存在；
- 用户尚未在全链冻结后明确批准“开始 M2 第一轮筛选”。

因此未生成 12 个正式 seed，未物化 720 个正式 case，也未提出数据驱动的第二轮。

## 当前能回答的问题

- 半径是否可在 M2 层缩减：**不能下结论**；
- 弹簧刚度有效区间是否出现：**不能下结论**；
- 针径趋势是否稳健：解析柔顺的 \(d^{-4}\) 趋势通过，但随机路径性能趋势
  **不能下结论**；
- 安装角是否可冻结：**不能**；按项目边界必须至少保留到 M3 结合阵列载荷共享；
- 下一轮最小必要仿真：门禁满足后，按冻结草案运行同一基准配方的 12 个配对
  seed、60 个硬件构型、100 mm 路径和 10 μm 生产地形。

一次性分析脚本已准备在 `scripts/analyze_m2_round1.py`。它只读取正式结果，不会
自动启动第二轮。
