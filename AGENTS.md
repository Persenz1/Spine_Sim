# Spine_Sim：后续编码任务约束

**2026-09-11最新迁移指示：用户要求暂停本机并迁移Windows服务器。当前已自然暂停，4149例入账，自动接续和定时监控均已关闭，不得自动恢复本机。离线包位于F:\IJMS_Server_Package，包含Python、全部现有地形及断点；服务器由用户从START.bat手动启动，可选择新增case数和自动并发，不再限8小时。只复用现成地形，不依赖GPU，不生成新地形。见[服务器迁移说明](docs/IJMS服务器迁移.md)。**

**最新指示优先：按约8小时分波执行。到时停止派发新case，所有已派发case自然结束，不强制中断；收尾后汇报，等待用户明确确认再手动启动下一波。当前仿真已暂停，未获得新的启动指示，不得因读取交接提示词、恢复会话或完成检查而自动启动。地形新生成必须CUDA，CUDA不可用就停止，不回退CPU。** 清理旧测试数据和交接整理已获授权；正式批次的输入、结果与SQLite进度保留。

新会话先读[当前运行交接](docs/IJMS运行交接.md)。旧测试已归拢至`E:\TestData\IJMS\old_tests_pending_delete`，自动递归删除被工具策略拦截，不能宣称已经删除；正式输入和结果独立保存在`production_151200`。

**最新执行授权：用户已明确要求“直接跑15.12万吧，你预估一下时间”。** 已授权启动2520设计×3预载×5材料×4独立地形实现的151200case，覆盖此前等待启动指示的限制，无需再次确认。入口为`run_ijms_production.py`，配置`experiments/ijms_coarse_151200.json`，输出`E:\TestData\IJMS\production_151200`。允许按同一冻结配置恢复未完成队列；不把中止或数值失败记成成功，不扩大到其他研究范围。运行中不修改力学实现后混入同一批次。

仪表盘只展示当前批次进度、运行状态和成功/提前终止/错误数量，不展示曲线、模型解释或“近似”标签。内部结果保留模型版本、参数和真实终止状态；界面简化不代表试跑已通过或精度已得到整体验证。

用户后续明确授权预载失败时换落点。允许按配置中的固定候选顺序重试，每个落点重新开始接近/预载；保存所有失败尝试与选中坐标。只在预载阶段换点，拖动中途停止不换点、不拼接轨迹；不挑选阻力最大的落点。该授权覆盖下文原固定落点限制，运行范围按上述151200case授权执行。

**2026-09-11最新用户指示覆盖默认执行策略：精度与速度折中，允许机制降阶；必要时针杆视为刚体，安装弹簧仍保留。** 用户随后明确认为15秒完成5250case的纯趋势版简化过度，因此 `run_ijms_trend.py` 的数据仅为粗筛参考，不是最终批量模型。当前用 `scripts/run_ijms_balanced_contact.py` 对照刚性针与凝聚弹性针，保留3D接触、分载和摩擦历史，先测少量完整路径的耗时与差异。不得自动恢复336000次严格求解或把旧纯趋势数据当作已满足折中要求。执行前读[本轮执行规划](docs/IJMS本轮执行规划.md)；下文约束继续适用于原严格模型，近似结果单独标识。

用户已授权 DeepSeek 配合 harness 优化程序逻辑、性能和工程实现；物理模型、边界条件和指标语义保持锁定。锁的是含义，不是文件。除用户后续明确要求改变物理外，按下述范围自主完成编码，不为普通重构反复请求确认。

**职责固定：后续编码/执行模型只负责程序实现、既定配置的批量执行和运行监控。** 物理规则、参数替代方法、材料/反演取值方案、研究扫描范围和结论口径由本轮科研定稿及用户指示确定。后续模型不能自行担当科研决策者、重新选物理参数或扩展论文结论。

## 开始工作

最新分流已经由用户明确：**带弹簧默认刚性针，纯固定安装保留弹性针。** 入口 `run_ijms_balanced_contact.py` 默认 `--needle-model auto`。不要重新退回15秒纯趋势模型，也不要对固定刚体多点接触擅自均分载荷。

1. 先读 [IJMS物理约束](docs/IJMS物理约束.md)。它是后续编码必须保持的约束，不能为使程序跑通而改写。
   小阵列执行先读[启动说明](docs/IJMS小阵列启动.md)和[固定粗筛配置](experiments/ijms_small_array.json)，使用scripts/run_ijms_small.py。变截面针、固定安装及梯度补偿已实现：r50/100 μm、渐细段2 mm、主体直径1 mm；均匀角露出4 mm，梯度以爪跟4 mm补偿。角度50/60/70/80°，刚度100/400/800/1000/2000/5000 N/m，额定行程4 mm，间距4/5/6 mm，实际针杆16段。只做阵列研究，先小阵列后决定大阵列（上限30×30）；[科学规划](docs/IJMS大规模扫描规划.md)中未编译的后续组不能自行执行。旧资料只参考组织思路，不使用旧结论或排名。
2. 按需读 [新版机理与接口](docs/IJMS新版机理与接口.md)、[批量仿真交接](docs/IJMS批量仿真交接.md)。新版入口是 `spine_sim.ijms:run_case`；旧 `single_spine.py` / `array.py` 是对照模型，不能替换新版入口。
3. 读取当前 `git status`，保留已有改动。本目录是 `D:\Code\Spine_Sim`，不是 V3 仓库；不自行提交、推送或迁移历史结果。

## 可以自主完成

修复实现错误、重构、向量化、缓存、内存与 I/O 优化、并行调度、按既定设计/样本表生成配置、恢复运行、日志、数值缩放、等价导数/线性代数实现、测试与文档维护。允许改力学文件中的代码，只要保持物理方程、约束、加载历史、接触支路和输出含义。若发现实现违背已明确的物理约束，可直接修复实现并说明依据。

不能用更改物理假设、隐藏失败、扩大容差、删断言或改参考答案来消除报错。用户允许公式、资料、数值反演和情景参数替代难以实测的输入；后续模型执行已给定的公式、来源值、反演目标与参数边界，不自行选择替代关系、材料数值、参考数据、扫描区间或论文阈值。已声明数值输入齐全时，不能以“尚未逐项实测/标定”为由停工。缺少具体输入时报告具体键及所影响任务，不猜值、不改口要求补齐实测；继续其他已有完整配置的工作。估计/数值等效不得冒充实测或唯一真实参数。具体方法见批量交接第 8 节。

上述参数取值授权不改变物理方程。新增物理机制、改变接触或摩擦定律、改变真实装配与加载协议，需有用户明确指示；未闭合的物理问题单独记录，继续不依赖它的编码工作。

## 跑批与监控范围

本轮预载已由用户固定：小阵列总P=0.5/1/2 N，大阵列总P不超过10 N；大阵列具体新增档位仍待小阵列结果后的科研计划。材料主表保留混凝土、红砖、砂纸P40/P100/P240；受控形貌是独立机理组。公开砂纸小扫描的统计合成不得标为新增实测样片。

按已交付 campaign、设计表、样本表和预算执行。可以调整 worker 数、任务分片、并发节奏与等价存储方式，观察进度、退出状态、求解残差、耗时、内存和磁盘占用，按既定恢复机制重启或继续自己的任务。执行异常可定位和修复；物理/几何范围停止如实保留，不反复重跑或偷偷放松条件来提升完成率。不得自行扩大样本量、重采样落点、更改物理参数、行程、阈值或保真度。允许汇总既定指标与运行事实，机理解释和论文结论留给科研定稿。

## 精简验证与交付

按物理约束文档选择与修改直接相关的现有测试；纯文档改动不启动物理仿真。只在具体风险或失败需要时增加针对性检查，不默认全量回归、长跑批量或另建审计框架。物理测试的方程、方向、状态及容差依据不能为了适配错误实现而修改；允许保留原物理断言的测试重构。

用户已明确将本项目仿真的过程产物和结果统一放在 `E:\TestData\IJMS`，此处覆盖原通用临时路径约定：`campaigns/`为生成配置，`surfaces/`为共享地形，`results/`为仿真结果，`logs/`为日志，`tmp/`为可清理的临时缓冲。不得清理整个IJMS目录或已保存的研究数据。源码与已有环境留在仓库，PDrive只读输入或保存用户要求的最终交付。

如果 harness 不自动读取 AGENTS.md，必须由 harness 把本文件及 `docs/IJMS物理约束.md` 的内容加入每次编码任务上下文。这是项目约束，不是文件系统只读保护。
